<%= form_for(@relationship_status) do |f| %>
  <% if @relationship_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@relationship_status.errors.count, "error") %> prohibited this relationship_status from being saved:</h2>

      <ul>
      <% @relationship_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
